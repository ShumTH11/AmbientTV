apiVersion: v1
kind: Service
metadata:
  name: ambienttv-backend
  namespace: ambienttv
  labels:
    app: ambienttv-backend
spec:
  selector:
    app: ambienttv-backend
  ports:
    - port: 80
      targetPort: 3000
      name: http
  type: ClusterIP
