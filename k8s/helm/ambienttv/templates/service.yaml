apiVersion: v1
kind: Service
metadata:
  name: {{ include "ambienttv.fullname" . }}
  labels:
    {{- include "ambienttv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ambienttv.selectorLabels" . | nindent 4 }}
